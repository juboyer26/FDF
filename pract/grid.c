#include "../includes/fdf.h"

int		grid(int sizex, int sizey, int sizez)
{
	int fd;
	char *line;
	int sizet;

	sizey = 0;
	sizet = 0;
	fd = open("whatever file", O_RDONLY);
	GNL = get_next_line(fd, &line);

	while (GNL)
	{
		sizex = 0;
		if (sizex < )
		{
			sizex++;
			sizet++;
		}
		sizey++;
	}
}

int fill()
{
}
